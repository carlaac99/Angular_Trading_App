export interface Quote {
    symbol: string;               
    high: number;          
    low: number;           
    lastPrice: number;     
    volume: number;       
    latestTradingDay: string;  
    previousClose: number;       
}
